# helm repo add eks https://aws.github.io/eks-charts
# helm search repo eks/aws-load-balancer-controller --versions
# helm upgrade --install aws-load-balancer-controller eks/aws-load-balancer-controller --namespace kube-system --version 1.13.3 --values 01-values.yaml --dependency-update

# The name of the Kubernetes cluster. A non-empty value is required
clusterName:  "pmulani-prod-eks-eu-west-1"
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  name: aws-load-balancer-controller
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::832850273244:role/pmulani-api-aws-controller-irsa-role